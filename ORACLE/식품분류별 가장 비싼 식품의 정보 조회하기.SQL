SELECT
CATEGORY
,PRICE AS MAX_PRICE
,PRODUCT_NAME
FROM FOOD_PRODUCT A
GROUP BY CATEGORY,PRICE,PRODUCT_NAME
HAVING (PRICE = (SELECT MAX(PRICE) FROM FOOD_PRODUCT B WHERE A.CATEGORY = B.CATEGORY)
       AND CATEGORY IN('과자', '국', '김치', '식용유'))
ORDER BY MAX_PRICE DESC;
