SELECT 
A.PRODUCT_ID
,A.PRODUCT_NAME
,SUM(B.AMOUNT*A.PRICE) AS TOTAL_SALES
FROM FOOD_PRODUCT A
, FOOD_ORDER B
WHERE 1=1
AND A.PRODUCT_ID = B.PRODUCT_ID
AND TO_CHAR(PRODUCE_DATE,'YYYYMM') = '202205'
GROUP BY A.PRODUCT_ID, A.PRODUCT_NAME
ORDER BY TOTAL_SALES DESC, A.PRODUCT_ID ASC;
